# Issuer server

## Setup

Install dependencies:

```
npm i
```

## Run

Run server with:

```
node index.js PORT
```

- `PORT` optional server port

## Develop

Run for development:

```
npx nodemon index.js PORT
```

- `PORT` optional server port
